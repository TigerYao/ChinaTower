"use strict";

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _path() {
  const data = require("path");

  _path = function _path() {
    return data;
  };

  return data;
}

function _child_process() {
  const data = require("child_process");

  _child_process = function _child_process() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// process.env.UMI_UI = 'none';
process.env.IS_ALITA = 'true';
process.env.UMI_PRESETS = require.resolve('@alitajs/umi-presets-alita');
const p = process.argv.slice(2); // 这里做了拦截，因为version命令冲突了

if (p[0] === 'version' || p[0] === '-v' || p[0] === '-version') {
  p[0] = 'alitaVersion';
}

process.env.ALITA_NOW_COMMAND = p[0];
process.env.ALITA_DIR = (0, _path().dirname)(require.resolve('../package'));

const umiBinPath = require.resolve('umi/bin/umi');

(0, _child_process().fork)(umiBinPath, p, {
  stdio: 'inherit'
});