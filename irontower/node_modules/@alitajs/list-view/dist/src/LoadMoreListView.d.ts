import React, { FC } from 'react';
import { ListViewProps } from 'antd-mobile/es/list-view';
/**
 * 像素转换
 * @param {Number} px - 750视觉稿像素
 * @return {Number} 屏幕上实际像素
 */
export declare function px2hd(px: number): number;
interface AliasProps {
    data?: string;
    pageSize?: string;
    offset?: string;
    total?: string;
    page?: string;
}
interface LoadMoreListViewProps extends Omit<ListViewProps, 'renderFooter' | 'onEndReached' | 'pullToRefresh' | 'dataSource'> {
    height?: string;
    isTabsPage?: boolean;
    alias?: AliasProps;
    container?: any;
    requestFunc: (params: any) => Promise<any>;
    requestParams?: object;
    renderRow: (rowData: any, sectionID: string | number, rowID: string | number, highlightRow?: boolean) => React.ReactElement<any>;
    renderFooter?: (noMore: boolean, loadingMore: boolean, loadMore?: () => void) => React.ReactElement<any>;
    noData?: React.ReactNode | string;
}
declare const LoadMoreListView: FC<LoadMoreListViewProps>;
export default LoadMoreListView;
