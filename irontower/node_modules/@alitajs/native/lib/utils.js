"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.checkPluginInstalled = checkPluginInstalled;
exports.checkDependenceInstalled = checkDependenceInstalled;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _path() {
  const data = require("path");

  _path = function _path() {
    return data;
  };

  return data;
}

function _fs() {
  const data = _interopRequireDefault(require("fs"));

  _fs = function _fs() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function checkPluginInstalled(rootPath, plugin) {
  const pluginsFetchPath = (0, _path().join)(rootPath, 'plugins/fetch.json');
  if (!_fs().default.existsSync(pluginsFetchPath)) return false;

  const plugins = require(pluginsFetchPath);

  return plugin.cordova in plugins;
}

function checkDependenceInstalled(rootPath, dependence) {
  const packagePath = (0, _path().join)(rootPath, 'package.json');

  const pkg = require(packagePath);

  const installedDependencies = pkg.dependencies || {};
  return dependence in installedDependencies;
}