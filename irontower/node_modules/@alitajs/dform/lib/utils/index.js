"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resetLabel = exports.getByteLen = exports.dateChange = exports.changeDateFormat = void 0;

var _moment = _interopRequireDefault(require("moment"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * 时间展示类型改变事件
 * @param val
 */
var changeDateFormat = function changeDateFormat(val, modeType) {
  var dateFormat = '';

  switch (modeType) {
    case 'datetime':
      dateFormat = (0, _moment.default)(val).format('YYYY-MM-DD HH:mm');
      break;

    case 'month':
      dateFormat = (0, _moment.default)(val).format('YYYY-MM');
      break;

    case 'time':
      dateFormat = (0, _moment.default)(val).format('hh:mm');
      break;

    case 'year':
      dateFormat = (0, _moment.default)(val).format('YYYY');
      break;

    default:
      dateFormat = (0, _moment.default)(val).format('YYYY-MM-DD');
      break;
  }

  return dateFormat;
};

exports.changeDateFormat = changeDateFormat;

var dateChange = function dateChange(date) {
  var stringDate = (0, _moment.default)(date).format('YYYY-MM-DD-HH-mm-ss');
  var dateList = stringDate.split('-');
  var numberDateList = dateList.map(function (item) {
    return parseInt(item, 10);
  });
  return new Date(numberDateList[0], numberDateList[1] - 1, numberDateList[2], numberDateList[3], numberDateList[4], numberDateList[5]);
};

exports.dateChange = dateChange;

var getByteLen = function getByteLen(val) {
  var len = 0;
  val.split('').forEach(function (item) {
    // eslint-disable-next-line no-control-regex
    if (item.match(/[^\x00-\xff]/gi) != null) {
      len += 2;
    } else {
      len += 1;
    }
  });
  return len;
};

exports.getByteLen = getByteLen;

var resetLabel = function resetLabel() {
  var list = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var placeholderList = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  // switch (list.length) {
  //   case 0:
  //     list.push('请选择省');
  //     break;
  //   case 1:
  //     list.push('请选择市');
  //     break;
  //   case 2:
  //     list.push('请选择区/县');
  //     break;
  //   case 3:
  //     break;
  //   default:
  //     break;
  // }
  // return list;
  if (list.length === placeholderList.length) return list;
  list.push(placeholderList[list.length]);
  return list;
};

exports.resetLabel = resetLabel;